import { FederationGraph } from "@/components/federation/federation-graph";

export default function FederationPage() {
  return (
    <div className="h-[calc(100vh-4rem)] w-full">
      <FederationGraph />
    </div>
  );
}
